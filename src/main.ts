import { App } from "./app";
// import { botCommands } from "./bot/bot.commands";

const app = new App();

app.init();
// for (const command of botCommands) {
//     console.log(command.toString().slice(3).slice(0, -1));
// }
